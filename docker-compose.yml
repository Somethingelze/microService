
services:
  postgres:
    image: postgres:15
    container_name: micro-service-db
    restart: always
    environment:
      POSTGRES_USER: some
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: micro_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    container_name: micro-service-app
    ports:
      - "8080:8080"
    depends_on:
       - postgres

    restart: on-failure
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/micro_db
      SPRING_DATASOURCE_USERNAME: some
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_FLYWAY_ENABLED: true
      JWT_SECRET: ${JWT_SECRET}
      JWT_ACCESS-TTL-HOURS: 1
      JWT_REFRESH-TTL-DAYS: 3


volumes:
  postgres_data: